"""LangGraph placeholder for the foundation decision flow.

The synchronous foundation slice in `sunshine_core.thin_slice` is the current
executable contract. This module is reserved for the LangGraph version of the
same bounded flow once persistence and Temporal activities are wired in.
"""


def build_foundation_graph():
    raise NotImplementedError("LangGraph foundation graph is planned after the contract slice settles.")
